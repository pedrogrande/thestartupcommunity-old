<%= simple_form_for(@profile_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

    <%= f.submit 'Create profile type', :class => "btn btn-success" %>
<% end %>
